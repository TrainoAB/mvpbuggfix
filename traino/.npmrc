# Use a specific registry
registry=https://registry.npmjs.org/

# Configure strictness rules
strict-ssl=true

# Prevent package-lock from being created (if not needed)
package-lock=false

# Save exact versions of installed dependencies
save-exact=true

# Configure pnpm if transitioning
prefer-frozen-lockfile=true

auto-install-peers=true
