import './page.css';

export default function ProfileGroup({ params }) {
  // Get the category name from the slug(current page)
  console.log(params);

  return (
    <main>
      <h1>Hello ProfileGroup</h1>
    </main>
  );
}
